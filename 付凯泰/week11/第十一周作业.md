# task01

```python
"""
1: 安装openai-agents框架，实现如下的一个程序：
• 有一个主agent，接受用户的请求输入，选择其中的一个agent 回答
• 子agent 1: 对文本进行情感分类
• 子agent 2: 对文本进行实体识别
"""
import os

# https://bailian.console.aliyun.com/?tab=model#/api-key
os.environ["OPENAI_API_KEY"] = "sk-b220c23d00d84424a07f2cf000e48920"
os.environ["OPENAI_BASE_URL"] = "https://dashscope.aliyuncs.com/compatible-mode/v1"

import asyncio
import uuid

from openai.types.responses import ResponseContentPartDoneEvent, ResponseTextDeltaEvent
from agents import Agent, RawResponsesStreamEvent, Runner, TResponseInputItem, trace
# from agents.extensions.visualization import draw_graph
from agents import set_default_openai_api, set_tracing_disabled
set_default_openai_api("chat_completions")
set_tracing_disabled(True)

# 子agent 1: 情感分类
sentiment_agent = Agent(
    name="sentiment_agent",
    model="qwen-max",
    instructions="你是一个情感分类专家。请分析用户输入文本的情感倾向，判断是正面、负面还是中性情感，并给出简短的理由。回答时先表明你的身份。"
)

# 子agent 2: 实体识别
entity_agent = Agent(
    name="entity_agent",
    model="qwen-max",
    instructions="你是一个实体识别专家。请从用户输入的文本中识别出命名实体（如人名、地名、组织机构、时间、数字等），并以列表形式展示识别结果。回答时先表明你的身份。"
)

# 主agent: 路由分发
triage_agent = Agent(
    name="triage_agent",
    model="qwen-max",
    instructions="你是一个路由助手。根据用户的请求内容，将其分配给合适的子agent：\n"
                 "- 如果用户想要分析文本的情感（如判断正面/负面/中性），请转交给 sentiment_agent\n"
                 "- 如果用户想要识别文本中的实体（如人名、地名、组织等），请转交给 entity_agent",
    handoffs=[sentiment_agent, entity_agent],
)


async def main():
    conversation_id = str(uuid.uuid4().hex[:16])

    msg = input("你好！我可以帮你进行情感分析和实体识别，请输入你的问题：")
    agent = triage_agent
    inputs: list[TResponseInputItem] = [{"content": msg, "role": "user"}]

    while True:
        with trace("Task01 Agent Routing", group_id=conversation_id):
            result = Runner.run_streamed(
                agent,
                input=inputs,
            )
            async for event in result.stream_events():
                if not isinstance(event, RawResponsesStreamEvent):
                    continue
                data = event.data
                if isinstance(data, ResponseTextDeltaEvent):
                    print(data.delta, end="", flush=True)
                elif isinstance(data, ResponseContentPartDoneEvent):
                    print("\n")

        inputs = result.to_input_list()
        print("\n")

        user_msg = input("请输入下一条消息（输入 'quit' 退出）: ")
        if user_msg.lower() == 'quit':
            break
        inputs.append({"content": user_msg, "role": "user"})
        agent = result.current_agent


if __name__ == "__main__":
    asyncio.run(main())
```

![image-20260416162711397](C:\Users\86182\AppData\Roaming\Typora\typora-user-images\image-20260416162711397.png)

# task02

```markdown
作业2: 4-项目案例-企业职能助手，增加3个自定义的tool 工具，实现自定义的功能，
并在对话框完成调用（自然语言 -》 工具选择 -》 工具执行结果）
```

```python
@mcp.tool
def get_ip_location(ip_address: Annotated[str, "IP address (e.g., '8.8.8.8' or '114.114.114.114')"]):
    """Retrieves the geographic location and ISP information for a given IP address."""
    try:
        return requests.get(f"https://whyta.cn/api/tx/ip?key={TOKEN}&ip={ip_address}").json()["result"]
    except:
        return []


@mcp.tool
def calculate_age(birth_date: Annotated[str, "Birth date in YYYY-MM-DD format (e.g., '1990-01-15')"]):
    """Calculates age and zodiac sign based on the given birth date."""
    try:
        from datetime import datetime
        birth = datetime.strptime(birth_date, "%Y-%m-%d")
        today = datetime.now()
        age = today.year - birth.year - ((today.month, today.day) < (birth.month, birth.day))

        zodiac_signs = [
            ("摩羯座", (1, 1), (1, 19)), ("水瓶座", (1, 20), (2, 18)),
            ("双鱼座", (2, 19), (3, 20)), ("白羊座", (3, 21), (4, 19)),
            ("金牛座", (4, 20), (5, 20)), ("双子座", (5, 21), (6, 21)),
            ("巨蟹座", (6, 22), (7, 22)), ("狮子座", (7, 23), (8, 22)),
            ("处女座", (8, 23), (9, 22)), ("天秤座", (9, 23), (10, 23)),
            ("天蝎座", (10, 24), (11, 22)), ("射手座", (11, 23), (12, 21)),
            ("摩羯座", (12, 22), (12, 31))
        ]

        zodiac = "未知"
        for sign, (start_m, start_d), (end_m, end_d) in zodiac_signs:
            if (birth.month, birth.day) >= (start_m, start_d) and (birth.month, birth.day) <= (end_m, end_d):
                zodiac = sign
                break

        return {"age": age, "zodiac_sign": zodiac, "birth_date": birth_date}
    except:
        return []


@mcp.tool
def translate_text(text: Annotated[str, "Text to translate"],
                   source_lang: Annotated[str, "Source language code (e.g., 'zh' for Chinese)"] = "zh",
                   target_lang: Annotated[str, "Target language code (e.g., 'en' for English)"] = "en"):
    """Translates text between different languages using external translation API."""
    try:
        return requests.get(
            f"https://whyta.cn/api/tx/fanyi?key={TOKEN}&content={text}&from={source_lang}&to={target_lang}"
        ).json()["result"]["trans_result"][0]["dst"]
    except:
        return []
```

1. ```markdown
  新增的 3 个工具说明
  
  1. get_ip_location(ip_address) - IP 地址查询
     功能：查询 IP 地址的地理位置和运营商信息
     参数：ip_address - IP 地址（如 "8.8.8.8"）
     返回：包含省份、城市、运营商等信息
     应用场景：网络安全分析、访问日志分析、用户地域统计
  2. calculate_age(birth_date) - 年龄和星座计算
     功能：根据出生日期计算年龄和星座
     参数：birth_date - 出生日期（格式："YYYY-MM-DD"）
     返回：包含年龄、星座、出生日期
     应用场景：员工信息管理、用户画像、生日提醒
  3. translate_text(text, source_lang, target_lang) - 文本翻译
     功能：多语言文本翻译
     参数：
     text - 要翻译的文本
     source_lang - 源语言（默认 "zh" 中文）
     target_lang - 目标语言（默认 "en" 英文）
     返回：翻译后的文本
     应用场景：跨国业务沟通、文档翻译、多语言支持
  ```

  

![image-20260416211602141](C:\Users\86182\AppData\Roaming\Typora\typora-user-images\image-20260416211602141.png)