作业1: 参考 04_Pydantic与Tools.py 中的tools的实现，搭建出一个文本翻译智能体，自动识别需要翻译的文本（原始语种、目标语种，待翻译的文本）。
帮我将good！翻译为中文 -》 原始语种、目标语种，待翻译的文本


class Text(BaseModel):
    """文本问答内容解析"""
    source: Literal["中文", "英文", "法文", "其他"] = Field(description="原始语种")
    target: Literal["中文", "英文", "法文", "其他"] = Field(description="目标语种")
    content: List[str] = Field(description="待翻译文本")
result = ExtractionAgent(model_name = "qwen-plus").call('帮我将你好北京翻译为法文。', Text)
print(result)


作业2:参考今天部署过程，自己实现一下04-government-advanced-rag的部署和测试过程；
步骤：
    - 下载模型，如果使用本地的模型，则需要下载。此外还需要bge embeeding
    - 配置本地环境，如es、fastapi
    - 命令行启动，测试

    1、服务器：gpu （RTX 3090 级别， 24GB显存）
        阿里云 / 腾讯云 ： 比较正规，有公网ip
        autodl ： 没有ip
    2、配置环境
        pytorch、vllm（本地大模型部署）、mysql、es

    >>> import torch
    >>> torch.cuda.is_available()

    vllm serve models/Qwen/Qwen3.5-0.8B/

    curl http://localhost:8000/v1/chat/completions -H "Content-Type: application/json" -d '{
    "model": "models/Qwen/Qwen3.5-0.8B/",
    "messages": [
    {"role": "assistant", "content": "你好我是Qwen"},
    {"role": "user", "content": "如何学习机器学习？"}
    ]
    }'

    3、项目代码传输上去，尝试启动

    pip install -r requirements.txt 
    python main.py

    4、验证下项目代码的完备性

    fastapi 接口文档地址，直接测试
