https://help.aliyun.com/zh/document_detail/421160.html?spm=a2c4g.11186623.0.0.30f638fe8lQYuY
功能 1: faq 类⽬管理，⼀级类⽬，⼆级类⽬， 对应的 faq 信息
https://help.aliyun.com/zh/document_detail/421161.html?spm=a2c4g.11186623.0.0.30f67558YDiTwC
功能 2: faq 录⼊和管理，待选提问、相似提问、⽣效时间、对应回答
使⽤场景：代替⼈⼯客服，⽤户的提问直接与历史的提问进⾏相似度匹配，最相似的提问的回答返回给⽤户。
作业 1（400 字⽂档）: 阅读上⾯的客服⼯作台的说明，总结我们⾃⼰作为后端开发和算法开发的⻆⾊，我们需要做什么？
◦ 需要设计数据库吗？
◦ 需要使⽤什么模型？
◦ 如何使⽤使⽤ bert 的？
◦ 是否需要使⽤⼤模型？

```
    作为后端开发与算法开发，为实现 FAQ 系统替代人工客服、完成用户提问相似度匹配并返回相应回答的核心需求，
需从系统架构、数据层设计、算法模型落地等维度分工协作，完成全流程开发与落地，具体工作如下：

    后端开发：
		负责系统底层支撑与功能实现，首先需要设计适配的数据库，需设计类目表、FAQ 主表、相似问法表、关联问题表、标签表等，
	分别存储类目层级关系、FAQ 基础信息（ID、标题、答案、生效时间、类目归属等）、相似提问语料、关联问题关联关系、标签信息，
	同时预留测试 / 正式双环境数据隔离字段，保障数据存储的规范性与关联性。其次需开发双环境管理接口，实现测试 / 正式环境切换、
	FAQ 增删改查、类目无限级管理（一级 / 二级及下级创建、平级新增、编辑删除）、批量操作（导入 / 导出 / 删除）、
	发布中心同步等功能；开发数据检索接口，支持按 FAQ ID、关键词、类目、标签等多维度过滤查询；开发 FAQ 生命周期管理接口，
	实现生效 / 失效、类目转移等功能，同时保障接口的高可用与数据一致性。

    算法开发：
		负责相似度匹配的算法实现与模型落地，核心使用 BERT 模型，将用户提问与 FAQ 的标准问法、相似问法进行
	向量化编码，通过计算向量间余弦相似度完成匹配，选取 Top-1 匹配结果返回给用户；具体使用时，先对 FAQ 语料库中
	的标准问、相似问进行预处理（分词、去停用词），再通过 BERT 进行预训练与微调，得到高质量的语义向量，构建语料向量库，
	用户提问时实时编码并与向量库做相似度计算。现阶段无需使用大模型，基于 BERT 的语义匹配已能满足 FAQ 固定语料的
	相似度匹配需求，大模型的生成式能力并非该场景核心需求，暂不引入可降低系统复杂度与成本。

		此外，算法开发需配合后端完成模型工程化落地，提供相似度匹配的算法接口，与后端系统对接；同时优化匹配效率，
	针对语料库规模做向量库的检索优化，保障用户提问的实时响应；还需持续迭代 BERT 模型，基于用户实际提问的语料进行模型微调，
	提升匹配准确率，同时开发相似问法的自动挖掘功能，辅助运营人员丰富 FAQ 的相似问法语料。
```
)
